
// Import the calculateSum function from the sumArray module
const calculateSum = require('./sumArray');

// Test the function
const numbers = [1, 2, 3, 4, 5];
console.log(`Sum of the numbers: ${calculateSum(numbers)}`);
